{
  "version": "1.1.0",
  "description": "Registry semua tools yang tersedia untuk Vista Mora Agent",
  "tools": [
    {
      "name": "web_search",
      "description": "Cari informasi di web (dokumentasi Laravel, package, solusi bug)",
      "config": "./web_search.json",
      "enabled": true
    },
    {
      "name": "code_exec",
      "description": "Eksekusi perintah terminal (artisan, composer, npm, php)",
      "config": "./code_exec.json",
      "enabled": true
    },
    {
      "name": "file_ops",
      "description": "Operasi file (baca, tulis, edit, hapus, list directory)",
      "config": "./file_ops.json",
      "enabled": true
    }
  ]
}
